version: '3.8'

services:
  privacy-filter:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: fintech_privacy_filter
    ports:
      - "8050:8050"
    environment:
      - PORT=8050
    restart: always
